---

- name: Rook-ceph client setup
  hosts: minion
  remote_user: root
  become: yes
  any_errors_fatal: true
  tags:
    - storage
    - storage-clientonly
  roles:
    - { role: rook-ceph, install_type: client }
