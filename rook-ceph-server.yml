---

- name: Rook-ceph server setup
  hosts: master
  remote_user: root
  become: yes
  any_errors_fatal: true
  tags:
    - storage
    - storage-serveronly
  roles:
    - { role: rook-ceph, install_type: server }
